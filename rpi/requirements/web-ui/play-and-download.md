# Play and download a session

Play a session's `session.m4a` in the browser, and download it.

- A named session downloads under its name rather than `session.m4a`, which is most of
  the value once files leave the Pi ([session naming](name-session.md)).
- Playback shows elapsed and total time against a progress indicator.

